<UserControl x:Class="HEMA.WpfApp.Controls.CalcBracketsPopupContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

	<Grid HorizontalAlignment="Center"
          VerticalAlignment="Top"
          Margin="0,20,0,0">
		<Border Background="White"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Width="400"
                Opacity="0">
			<Border.Triggers>
				<EventTrigger RoutedEvent="Loaded">
					<BeginStoryboard>
						<Storyboard>
							<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="-30"
                                             To="0"
                                             Duration="0:0:0.25"
                                             DecelerationRatio="0.8"/>
							<DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0"
                                             To="1"
                                             Duration="0:0:0.25"/>
						</Storyboard>
					</BeginStoryboard>
				</EventTrigger>
			</Border.Triggers>

			<Border.RenderTransform>
				<TranslateTransform Y="0"/>
			</Border.RenderTransform>

			<StackPanel>
				<TextBlock Text="Рассчитать сетку"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

				<StackPanel Orientation="Horizontal" Margin="0,5">
					<TextBlock Text="Число прошедших участников:" Width="180" VerticalAlignment="Center"/>
					<TextBox x:Name="ParticipantCountBox"
                             Width="175"
                             Height="30"
                             VerticalContentAlignment="Center"
                             Padding="5"/>
				</StackPanel>

				<StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,15,0,0">
					<Button Content="Рассчитать"
                            Width="120"
                            Margin="0,0,10,0"
                            Background="LightGreen"
                            HorizontalContentAlignment="Right"
                            Tag="↵"
                            Click="ApplyButton_Click"/>

					<Button Content="Отмена"
                            Width="100"
                            Background="LightGray"
                            HorizontalContentAlignment="Right"
                            Tag="Esc"
                            Click="ResetButton_Click"/>
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>