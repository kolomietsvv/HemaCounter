<Window x:Class="HEMA.WpfApp.PopupTimeSpanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HEMA.WpfApp"
        Title="Время" Width="450" Height="200"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#DD1E1E1E" Foreground="White"
        Topmost="True" ShowInTaskbar="False"
        AllowsTransparency="True" Opacity="0.98"
        ShowActivated="False"
        Loaded="Window_Loaded"
		KeyDown="Window_KeyDown">
	<Window.Resources>
		<local:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter" />
		<Style TargetType="TextBox">
			<Setter Property="Margin" Value="0,12,0,0"/>
			<Setter Property="Padding" Value="12,10"/>
			<Setter Property="FontSize" Value="18"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
		</Style>
		<Style TargetType="Button">
			<Setter Property="Focusable" Value="False"/>
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="Padding" Value="12,8"/>
			<Setter Property="Margin" Value="0,0,10,0"/>
		</Style>
	</Window.Resources>

	<Border CornerRadius="16" Background="#FF2B2B2B" Padding="20" >
		<StackPanel>
			<TextBlock Text="Введите время (мм:сс)" Opacity="0.85" FontSize="18" HorizontalAlignment="Center"/>
			<TextBox x:Name="TimeBox" Width="120">
				<TextBox.Text>
					<Binding 
						Path="Value"
						Mode="TwoWay"
						UpdateSourceTrigger="Explicit"
						ValidatesOnDataErrors="True"
						NotifyOnValidationError="True"
						Converter="{StaticResource TimeSpanToStringConverter}"/>
				</TextBox.Text>
			</TextBox>

			<StackPanel Orientation="Horizontal" Margin="0,14,0,0" HorizontalAlignment="Center">
				<Button Content="−10 сек" Click="Minus10s_Click"/>
				<Button Content="+10 сек" Click="Plus10s_Click"/>
				<Button Content="OK" Width="100" Click="Ok_Click"/>
				<Button Content="Отмена" Width="100" Click="Cancel_Click"/>
			</StackPanel>

			<TextBlock Foreground="#F66" FontSize="14" Margin="0,8,0,0" TextAlignment="Center">
				<TextBlock.Style>
					<Style TargetType="TextBlock">
						<Setter Property="Text" Value="{Binding ValidationMessage}"/>
						<Style.Triggers>
							<DataTrigger Binding="{Binding HasError}" Value="False">
								<Setter Property="Visibility" Value="Collapsed"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</TextBlock.Style>
			</TextBlock>
		</StackPanel>
	</Border>
</Window>
